# MiniMaths 产品说明书（当前版本）

## 1. 产品概述
`MiniMaths` 是一个移动端优先的练习网站，包含四个子模块：

- `minimaths`：数学口算练习与排行榜
- `miniEng`：英语语音答题与 AI 评分
- `小古文`：古诗文填空练习
- `novel`：众创小说阅读、续写、投票与整合导出

产品支持本地偏好设置、服务端持久化数据、移动端交互优化，以及 Linux 服务器一键部署。

## 2. 目标用户与场景
- 学生/家长：日常口算练习、成绩记录
- 英语学习者：语音作答 + 反馈改进
- 文学兴趣用户：古文填空、众创小说协作
- 站点维护者：个人云服务器快速部署

## 3. 功能说明

## 3.1 导航系统
- 统一导航入口，支持跳转：
  - `minimaths`
  - `miniEng`
  - `小古文`
  - `novel`
- 采用共享导航加载组件 `nav-loader.js`，统一维护。

## 3.2 数学练习（minimaths）
### 题目与作答
- 默认进入练习页，直接显示题目。
- 题型支持：加、减、乘、除、加减、四则混合。
- 配置支持：
  - 数字位数：1~5 位
  - 数字个数：2/3/4（除法时仅保留 2）
  - 题目数量：10/20/30/40
- 数字键盘三列布局，含退格。
- 输入正确自动下一题；错误不提示。

### 历史记录
- 记录格式：题目 + 耗时。
- 大屏右侧展示；小屏折叠为悬浮面板。
- 历史条数与题目数量配置联动。

### 回合与排行榜
- 完成一轮后统计总时长并入榜。
- 完成后弹祝贺提示，自动跳转排行榜。
- 排行按配置分组（例如 `加-2-2-10`），组内按总时长升序，显示前 10 名。
- 支持查看某条成绩的详细题目耗时。

## 3.3 英语练习（miniEng）
### 题库与语音
- 从内置话题随机出题。
- 点击“回答”开始语音采集，限时 10 秒。

### AI 评分
- 评分维度：语法、发音、表达、综合。
- 提供者可选：OpenAI / DeepSeek / Qwen。
- API Key 和提供者配置仅保存在本地浏览器。
- AI 失败时自动回退到本地启发式评分。

## 3.4 小古文
- 随机抽取古诗文，生成填空题。
- 实时判定：正确为绿，错误为红。
- 全部答对后显示简短赏析与“下一首”。
- 作者展示包含朝代（如“唐·李白”）。

## 3.5 众创小说（novel）
### 阅读与翻页
- 采用树结构（`parent_id`）组织内容。
- 默认读取序号 1 的最高票内容。
- 上一页跳父内容；下一页跳当前内容子节点最高票。

### 投稿与投票
- 可续写（<=400 字）并提交作者名（<=10 字）。
- 点赞写入数据库并限流（设备 + IP，30 秒）。

### 候选内容
- 默认显示第一名候选。
- `more` 弹窗支持分页加载（20 条/次，滚动加载）。
- 点击候选可直接跳转阅读。

### 本地个性化
- 可设置作者名、小说名、“我是作者”。
- “我要续写 / 我不要写”控制续写模块显示。
- 当前阅读内容 ID 存入 Cookie，刷新后自动定位。

### 跳转、分享与整合
- 支持按页序号或内容 ID 跳转。
- 不存在时弹窗内红字提示“该内容不存在”。
- 详情支持分享链接直达当前内容。
- 支持“整合”功能：从当前内容向上回溯最多 100 层（或到根）。
- 整合内容可弹窗查看并直接下载 PDF。

## 4. 数据与存储策略
- **线上（Vercel）**：Turso（`TURSO_DATABASE_URL` + `TURSO_AUTH_TOKEN`），排行榜、历史、用户、小说等均走云端库
- **本地开发**：SQLite 文件（`data/minimaths.db`），未配置 Turso 环境变量时使用
- 浏览器本地（Cookie / localStorage）：
  - 用户名
  - 数学设置
  - miniEng AI 配置
  - novel 本地设置与当前阅读 ID

## 5. 技术架构
- 前端：HTML/CSS/JavaScript（多页面）
- 后端：Node.js（ESM）+ `node:http`
- 数据库：线上 Turso（`@libsql/client`）；本地 `node:sqlite` 文件
- PDF：`pdfkit` + CJK 字体（Vercel 部署需自行提供字体路径或环境变量）
- 部署：Vercel（`vercel.json` → `api/index.js` → `server.mjs`）

## 6. 部署与运维（现状）
- Vercel 项目环境变量：`TURSO_DATABASE_URL`、`TURSO_AUTH_TOKEN`，以及 Stripe / AI 等按需配置
- 部署后数据持久在 Turso；勿随意更换 Turso 库 URL，否则会连到空库
- 本地调试：`node server.mjs`（默认 `http://localhost:3000`）

## 7. 版本亮点
- 移动端优先交互与响应式布局
- 数学/英语/古文/小说四模块融合
- 小说树状叙事 + 候选排序 + 反刷票
- 整合内容一键导出 PDF
- 托管于 Vercel，数据持久化于 Turso

## 8. 已知注意事项
- PDF 中文依赖可用 OTF/TTF 字体，部分 `.ttc` 在 `pdfkit` 下不兼容。
- 新增前端静态文件时，部署打包清单需要同步更新。
